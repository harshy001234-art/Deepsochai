kind = "web"
previewPath = "/"
title = "DeepSoch AI"
version = "1.0.0"
id = "artifacts/deepsoch"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21606

[services.development]
run = "pnpm --filter @workspace/deepsoch run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/deepsoch", "run", "build" ]
publicDir = "artifacts/deepsoch/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21606"
BASE_PATH = "/"
